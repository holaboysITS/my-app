export interface Plant {
    id: string
    name: string
    location: string
    description: string
    machineries: string[]
}

export interface PlantIn {
    name: string
    location: string
    description: string
}